{
  "name": "faktorio-monorepo",
  "private": true,
  "workspaces": [
    "faktorio-api",
    "faktorio-fe"
  ],
  "devDependencies": {
    "prettier": "^3.2.5",
    "tsx": "^4.7.1"
  },
  "scripts": {
    "dev": "cd faktorio-api && bun dev & cd faktorio-fe && bun dev",
    "kill": "kill -9 $(lsof -t -i:9189)",
    "db": "cd faktorio-api && pnpm generate && pnpm migrate",
    "bumpDeps": "ncu -u && cd faktorio-api && cd ../faktorio-fe && pnpm i"
  }
}
