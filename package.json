{
  "name": "faktorio-monorepo",
  "private": true,
  "workspaces": [
    "faktorio-api",
    "faktorio-fe"
  ],
  "devDependencies": {
    "prettier": "^3.2.5"
  },
  "scripts": {
    "dev": "cd faktorio-api && bun dev & cd faktorio-fe && bun dev",
		"kill": "kill -9 $(lsof -t -i:8787)"
  }
}
