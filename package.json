{
  "name": "faktorio-monorepo",
  "private": true,
  "workspaces": [
    "faktorio-api",
    "faktorio-fe"
  ],
  "devDependencies": {
    "prettier": "^3.4.2",
    "turbo": "^2.3.4",
    "typescript": "^5.7.3",
    "vitest": "^3.0.3"
  },
  "packageManager": "pnpm@9.15.4",
  "scripts": {
    "dev:fe": "cd faktorio-fe && bun dev",
    "dev:api": "cd faktorio-api && bun dev",
    "dev": "npm-run-all --parallel dev:*",
    "kill": "kill -9 $(lsof -t -i:9189)",
    "format": "prettier --write .",
    "db": "cd faktorio-api && pnpm generate && pnpm migrate",
    "dbm": "cd faktorio-api && pnpm migrate",
    "bumpDeps:api": "cd faktorio-api && ncu -u && pnpm i",
    "bumpDeps:fe": "cd faktorio-fe && ncu -u && pnpm i",
    "bump": "npm-run-all --parallel bumpDeps:*",
    "typecheck": "turbo tsc",
    "test": "vitest"
  },
  "dependencies": {
    "dayjs": "^1.11.13",
    "npm-run-all": "^4.1.5",
    "superjson": "^2.2.2"
  }
}
